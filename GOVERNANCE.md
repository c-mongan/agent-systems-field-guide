# Governance

The project favours small, reviewable changes. Architecture claims require evidence. Maintainers may reject changes that add a framework, agent, or protocol without a measured need.

Vendor mappings are updated independently from portable doctrine. A vendor change should not silently rewrite the core model.
